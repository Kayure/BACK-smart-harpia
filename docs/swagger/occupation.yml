/occupations:
  get:
    tags:
      - Occupation
    summary: Returns a list of occupations
    description:
    responses:
      '200':
        description: A JSON array of occupations
        content:
        application/json:
          schema:
            type: array
            items:
              type: string
